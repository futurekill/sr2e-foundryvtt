# SR2E — Backlog

Deferred ideas, captured so they aren't lost. Not prioritized; pull from here
when there's appetite. Nothing here blocks a release.

## UI / polish
- **Per-"class" UI themes (switchable).** Keep the current default theme as the
  default, but offer alternate themes a user can switch between (e.g. a
  street-sam / decker / mage flavour). Make it a user/client setting so it's
  per-player, not per-world. Default must stay the default. *(Low priority.)*
- **Flavor gear** — pocket phones, personal-computer tiers, entertainment
  electronics, working-gear kits from the core Gear chapter. Rarely used
  mechanically; only if we want a literal 1:1 of the chapter.
- **Bodyware / Cyberlimb cyberware sub-folders** — Headware is split
  (Communications/Eyes/Ears/Internals); Bodyware (13) and Cyberlimbs (5) are
  flat. Split only if we want visual symmetry.

## Content
- **Sourcebook modules' own completeness audits** — the core system pack is
  complete vs the core book; per-book modules (NAGRL, SSC, Rigger 2, Grimoire,
  etc.) each need their own pass for non-core gear/weapons/'ware.
- **Paranormal Animals of Europe** — a new critters module, a large from-scratch
  dig in its own repo. Needs a go-ahead before starting.

## Verification
- **Roll-table live check** — the Quench compendium batch confirms each table has
  result rows; nobody has watched one actually *roll* in a live world yet. ~30s
  manual check.
